const SIDE = ["buy", "sell"];

const PAIR = ["BTC/USDT", "ETH/USDT"];

const STATUS = ["pending", "executed", "failed"];

const DEFAULT_STATUS = STATUS[0];

module.exports = {
    SIDE,
    PAIR,
    STATUS,
    DEFAULT_STATUS
}